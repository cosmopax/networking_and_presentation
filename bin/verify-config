#!/bin/bash
if [ ! -f .env ]; then
    echo "Error: .env file not found."
    exit 1
fi

if grep -q "^NEXT_PUBLIC_STRIPE_KEY=" .env; then
    echo "NEXT_PUBLIC_STRIPE_KEY found in .env"
    exit 0
else
    echo "Error: NEXT_PUBLIC_STRIPE_KEY not found in .env"
    exit 1
fi
